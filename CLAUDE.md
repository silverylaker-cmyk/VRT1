# ENT WebXR 임상 앱 — 작업 핸드오프 (Claude Code용)

> 이 문서는 작업 컨텍스트를 이어가기 위한 핸드오프 메모입니다.
> 사용자: 대구 ENT 의원 원장. 용도: **병원 내 치료 전용**(상용 배포 아님).
> 기기 타깃: **Galaxy XR(Android XR / Chrome WebXR)** 우선, Vision Pro(Safari WebXR) 호환.
> 최종 갱신: 2026-07. 전정 앱 13세션(VOR 억제 포함) + 후각(장미) 배포. 6시간 주기 자가 검증·개선 루프 가동 중.

---

## 0. 가장 중요한 교훈 — WebXR 배포는 HTTPS 필수

세션에서 가장 오래 막혔던 지점. **반드시 먼저 숙지할 것.**

- WebXR 몰입 세션(`requestSession`)은 **보안 컨텍스트(https)** 에서만 열린다.
- `file://`, `http://`, **임베드 iframe**에서는 `requestSession`이 권한 단계 이전에
  `NotSupportedError: The specified session configuration is not supported` 로 실패한다.
  (`isSessionSupported`는 `true`를 반환해도 `requestSession`은 실패할 수 있음 — 둘은 별개.)
- **Android XR(Galaxy XR) Chrome은 모든 WebXR API에 "3D 매핑·카메라 추적" 권한을 요구**한다.
  https에서 세션을 요청해야 권한 프롬프트가 뜨고, 허용해야 진입된다.
- iframe 임베드 시 `allow="xr-spatial-tracking"` 필요.

### 배포 상태 (확정·운영 중)
- GitHub Pages 레포: **`silverylaker-cmyk/VRT1`**, `main` 브랜치 / 루트에서 서빙.
- 배포 URL: **`https://silverylaker-cmyk.github.io/VRT1/`** (Pages 활성화 완료).
- 작업 흐름: 기능 브랜치 → PR → `main` 머지 → Pages가 1~3분 내 재배포.
- 빠른 시험용 대안: Netlify Drop, Cloudflare Pages, 또는 `cloudflared`/`ngrok` https 터널.
- 단일 HTML 파일이면 충분(외부 에셋 없음, Three.js는 CDN 로드).

---

## 1. 현재 산출물 (배포 완료 2종)

둘 다 **단일 HTML + Three.js r128(CDN) + 수동 WebXR 세션** 구조.

### A. 후각 재훈련 — 장미 정원 (`rose-garden-olfactory.html`)
- URL: `…/VRT1/rose-garden-olfactory.html`
- 절차적 석양 장미 정원(그라데이션 하늘·잔디·산책로·인스턴스 장미 덤불/꽃·먼 나무·석양 글로우).
- **흡입 안내 타이머**: 준비 6s → (흡입 15s / 휴식 10s) ×N → 완료. 흡입·휴식 시간·반복 횟수 조절 가능.
- **호흡 안내 원**(카메라 자식): 흡입 시 확장(황금색), 휴식 시 수축(청록색), 단계 전환 신호음(Web Audio).
- 향은 **실물 키트**(사용자 보유)로 환자가 직접 흡입. 앱은 ①장면 ②타이밍/호흡 큐 담당.
- 핀치(select) = 시작/일시정지/재시작. 화면 미리보기(드래그) 지원.
- 자가평가 기록은 **미포함**(현재 의도적). 추후 옵션.

### B. 전정재활 — 멀티 세션 (`index.html` = VRT1 배포본)
- URL: `…/VRT1/` (루트). **장면 레지스트리 + 동적 옵션 UI + 공통 셸** 구조.
- **공통 셸**(모든 세션 공유): HUD(카메라 자식), **핀치=즉시 정지**(자극 페이드+화면 디밍+"정지됨"),
  점진적 시작(램프업), 세션 후 **0~10 어지럼 자가평가**(localStorage) + **CSV 내보내기**, 진단 줄.
- 현재 **13개 세션**(그룹별):
  - **습관화·광학흐름**: 마트 통로(군중 옵션) · 옵토키네틱(점/드럼, 좌우·상하·회전) · 전진/확장 광학흐름(터널) · 격자/체커보드(공간주파수·대비·드리프트)
  - **시선 안정화·안구운동**: 시선 안정화 VOR×1/×2(메트로놈 페이싱) · VOR 억제/캔슬레이션(머리 고정 표적=카메라 자식 + 월드 고정 배경점) · 추종·단속 안구운동 · 표적 맞히기(시선 드웰)
  - **시선 안정화·대체운동**: 능동 안구-머리 이동(두 표적, 눈먼저→머리) · 기억 표적(암전 속 기억 응시)
  - **환경 노출**: 번화가·횡단보도(횡단 보행자) · 에스컬레이터(상승/하강)
  - **시각 의존·균형**: 흔들리는 방(roll/pitch/sway 시각-전정 충돌)
- 임상가용 공통 설정: 자극 강도/속도(0~100%, 해당 세션), 세션 길이(1~10분), 램프업, 중앙 응시점.
- **임상 프리셋**: 세션+세부 설정+강도+길이를 localStorage에 저장/불러오기/삭제(환자별·단계별). 스키마 변경에 안전(defaults 병합).

---

## 2. WebXR / Three.js 기술 메모 (재현 주의점)

- **세션 요청 패턴(검증됨):** 옵션 없이 `immersive-vr`부터, 실패 시 `{optionalFeatures:['hand-tracking']}`, 그다음 `immersive-ar` 순으로 폴백. `local-floor`/`bounded-floor`는 Galaxy XR에서 거부될 수 있으니 **요청하지 말 것**.
- **참조 공간:** `renderer.xr.setReferenceSpaceType('local')` (좌식 기준, 호환성 최고). `local-floor`는 피한다.
- **XR에서 카메라 이동 금지:** XR 중엔 헤드셋이 카메라 포즈를 제어하므로 `camera.position`을 직접 바꿔도 무시됨. **대신 월드(콘텐츠 그룹)를 이동/회전**시켜 광학 흐름(vection)을 만든다.
- **HUD/오버레이/표적/응시점/레티클:** 캔버스 텍스처·메시를 **`camera`의 자식**으로 추가하면 시야에 고정. `scene.add(camera)` 필수. 단, **VOR/추종/대체운동의 "표적"은 월드 고정**이어야 머리 움직임이 시선각을 바꾼다(카메라 자식 금지).
- **입력:** 컨트롤러 없이 `session.addEventListener('select', ...)` = 핀치/탭. **게임형(표적 맞히기)도 select는 안전 정지로 유지**하고, 명중은 **시선 드웰**(카메라 forward와 표적 방향각 비교, `getWorldDirection`/`getWorldPosition`)로 처리.
- **루프:** XR 진입 시 `renderer.setAnimationLoop(render)`, 비-XR 미리보기는 `requestAnimationFrame`.
- **성능:** 군중·상품·점은 `InstancedMesh`/`Points`로(밀도 단계별 수 조절). 모바일급 GPU 과밀 주의.
- **끊김 없는 루프:** 콘텐츠를 주기 `PERIOD(6m)`로 반복 생성하고 `world.z`를 `PERIOD`로 wrap. 군중도 `PERIOD/spacing`이 정수가 되게 배치(원형 4종 반복)해 이음매 제거. 에스컬레이터는 경사 단위벡터 × (offset mod step).
- **멀티 세션 아키텍처(현 index.html):** `SCENES` 레지스트리 = `{name, group, useSpeed, useFixation, defaults, controls[], build(), update(dt,k,t), metric(), cue?()}`. `world` 하나를 세션마다 `clearGroup` 후 재빌드(이동/회전/텍스처 드리프트 등 세션별). `opts`=세션 옵션, `SS`=세션 런타임 참조(빌드 시 리셋). 설정 UI는 `controls` 기술자로 **동적 렌더**(range/seg/toggle). HUD 하단 안내는 `cue()` 훅(없으면 기본 안전문구; VOR은 페이싱 카렛 별도).
- **그 외 기법:** 전시야 패턴=`CanvasTexture`+`RepeatWrapping`+`repeat`/`offset` 드리프트(격자). 그라데이션 하늘=`SphereGeometry`(BackSide)+정점색. 암전=`scene.background.lerp(어둠)`+`hemi.intensity` 보간+표적/점 `visible` 토글(기억 표적). `clearGroup`은 **재귀 + material 해제**(다중 메시 정리).

---

## 3. 임상 설계 파라미터

### 후각(표준 Hummel 프로토콜 기반)
- 향 4종 기본: 장미·유칼립투스·레몬·정향. 향마다 장면 매칭(장미→정원, 유칼립투스→숲/스파, 레몬→과수원, 정향→향신료 시장).
- 흡입 ~10–15초 × 수회, 하루 2회, 12주. "냄새 맡으며 대상 시각화"가 핵심 → VR 장면이 후각-시각 연합 강화.
- 추가 커플링 후보: 커피→카페, 바닐라/빵→베이커리, 편백/소나무→편백 숲, 라벤더→라벤더 밭, 녹차→녹차밭, 백단향→산사. (실물 향 구득 용이성·삼차신경 자극취 회피 고려.)
- **현재 구현은 장미 정원 1종만.** (사용자 결정 — 당분간 확장 안 함.)

### 전정
- **습관화**: 통제된 광학 흐름 + 시각 복잡도의 점진적 등급화(사실감 아님). 낮은 강도→점진 증가.
- **시선 안정화(VOR×1/×2)**: 표적 응시 + 박자 맞춰 머리 회전. ×1 표적 고정, ×2 표적 역방향. VRT 표준 1차 운동.
- **대체운동(substitution)**: ①능동 안구-머리 이동(눈 먼저→머리) ②기억 표적(암전 속 기억 응시·중추 예측). 전정 소실/감소 보상.
- **환경 노출**: 번화가·에스컬레이터 등 실제 유발 환경 습관화.
- **시각 의존**: 흔들리는 방으로 시각-전정 충돌 도전(PPPD/시각성 어지럼).
- 공통 안전: 앉은 자세 기본, 보조자 동반, 즉시 정지, 금기군(급성 전정신경염·낙상 고위험) 선별.
- 근거: 시각성 어지럼/PPPD 환경 노출 습관화·VOR·대체운동은 정식 VRT 기법. VR이 균형·불안·증상에서 기존 운동요법 대비 우월 보고 있음.

---

## 4. ENT WebXR 앱 로드맵

1. 전정재활 — **배포·운영(VRT1, 13세션)**. 시선안정화(VOR×1/×2/억제)·대체운동 포함.
2. 후각 재훈련 — **배포(장미 정원)**.
3. BPPV 이석정복 가이드 + 3D 이석 교육 — 후보.
4. 이명 사운드테라피(주파수·강도 맞춤 + 몰입 환경, 공간음향) — 후보.
5. 시술 중 몰입 분산(외래 비강 내시경·진정 시술 — 미다졸람 절감 근거 있음) — 후보.
6. 공간 청취 훈련(보청기/인공와우) — 후보.
7. 3D ENT 해부 환자교육 — 후보.

---

## 5. 다음 작업(TODO)

### 완료
- [x] `index.html`(전정) Pages 배포 + 헤드셋 https 진입 경로 확보.
- [x] 마트 통로 군중 추가.
- [x] 옵토키네틱 줄무늬(드럼) 패턴.
- [x] 세션 종료 후 어지럼 0~10 자가평가(localStorage) + CSV 내보내기/기록 보기.
- [x] 시선안정화(VOR×1/×2), 전진/확장 광학흐름, 추종·단속, 격자/체커보드, 번화가, 에스컬레이터, 흔들리는 방, 표적 맞히기.
- [x] 대체운동(능동 안구-머리 이동, 기억 표적).
- [x] 공통 셸(장면 레지스트리 + 타이머/HUD/안전정지/기록) 추출·재사용.
- [x] 장미 정원 후각 앱 배포.

### 검증 필요(헤드셋 실측)
- [ ] 갤럭시 XR에서 각 세션 진입·체감 확인. 특히 **VOR 페이싱 속도감, 대체운동 전환/암전 길이, 에스컬레이터·흔들리는 방 자극 강도** 튜닝.
- [x] 표적 맞히기 시선 드웰 각도·시간 → **옵션으로 노출**(명중 판정 각도 2~7°, 응시 유지 0.4~1.5s). 실측하며 기본값 조정만 남음.

### 확장 후보
- [ ] 앱 진입용 랜딩/메뉴 페이지(전정·후각·향후 앱 선택).
- [ ] 기록 저장 IndexedDB 전환(대량·환자별 구분), 개인정보보호법 고려(로컬 우선).
- [ ] 360° 실사 에셋으로 현실감 업그레이드(후각/환경 노출).
- [x] 세션별 임상 프리셋 저장/불러오기(localStorage).

---

## 6. 레포/배포 메모
- WebXR 배포 레포: `silverylaker-cmyk/VRT1` → `https://silverylaker-cmyk.github.io/VRT1/`
- 파일 구성(현재, 루트 평면 배치):
  - `index.html` — 전정재활 멀티 세션(12종).
  - `rose-garden-olfactory.html` — 후각 재훈련 장미 정원.
  - `CLAUDE.md` — 본 핸드오프 문서.
- 기존 관련 레포: `silverylaker-cmyk/AAC`(AAC PWA).
- 앱이 더 늘면 하위 폴더(`/olfactory/`, `/vestibular/`)로 분리 + 루트에 랜딩 `index.html` 고려.
- 작업 규약: 기능 브랜치 → PR → `main` 머지. 머지된 PR은 재사용 불가(후속 작업은 새 브랜치/PR).
